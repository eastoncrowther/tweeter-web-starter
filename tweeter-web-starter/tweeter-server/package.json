{
  "name": "tweeter-server",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "npm run compile",
    "compile": "npm run clean && tsc --noEmit && npm run bundle",
    "bundle": "esbuild src/lambda/GetFollowerCountHandler.ts src/lambda/LoginHandler.ts --bundle --platform=node --target=node18 --outdir=dist",
    "clean": "rimraf dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "date-fns": "^3.3.1",
    "tweeter-shared": "file:../tweeter-shared",
    "uuid": "^9.0.1"
  },
  "devDependencies": {
    "esbuild": "^0.27.4"
  }
}
